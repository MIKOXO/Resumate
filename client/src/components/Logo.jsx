import { Link } from 'react-router-dom'

const Logo = () => (
  <Link to="/login" className="text-lg font-semibold text-primary hover:text-accent-hover transition-colors">
    Resumate
  </Link>
)

export default Logo
